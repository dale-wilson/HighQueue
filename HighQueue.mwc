workspace(*) {
  cmdline += -feature_file HighQueue.features
  specific(prop:microsoft) {
    cmdline += -expand_vars
  }
  src/HighQueue.mpc
  src/Components/Components.mpc

  specific(make) {
    cmdline += -value_template "configurations=Release Debug"
  }

}
